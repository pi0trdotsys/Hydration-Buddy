package com.kropi.hydration.ui

import androidx.compose.ui.graphics.Color

/**
 * Ported from the oklch() tokens in src/styles.css (.dark palette) so the
 * native widget matches the TS mockup's material/minimalist/tech-fancy look.
 */
object KropiColors {
    val background = Color(0xFF020F19)
    val card = Color(0xFF071A27)
    val secondary = Color(0xFF132938)
    val foreground = Color(0xFFEEF7FA)
    val mutedForeground = Color(0xFF91A9B3)
    val aqua = Color(0xFF00DFE8)
    val aquaDeep = Color(0xFF007BB2)
    val accent = Color(0xFF6D9AFF)
    val border = Color(0x1AFFFFFF)
}
